# Production environment — used by Vercel when deploying
# This file IS safe to commit (no secrets — it's just the public API URL)
VITE_API_URL=https://expense-tracker-production-9f9b.up.railway.app/api
